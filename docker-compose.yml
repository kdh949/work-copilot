services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: work-copilot-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: work_copilot
    ports:
      - "5432:5432"
    volumes:
      - work-copilot-postgres-data:/var/lib/postgresql/data

volumes:
  work-copilot-postgres-data:
